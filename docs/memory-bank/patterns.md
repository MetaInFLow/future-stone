# Patterns

- Artifact-first simulation: UI reads durable JSON/JSONL artifacts, not hidden in-memory state.
- Runner abstraction: replay first, PiAgent later, same `SkillRun` and `DecisionTrace` schema.

